# Local: Flask en vivo (default si no existe esta var)
VITE_API_MODE=live
# Produccion estatica (Vercel): JSON pre-generados en /api/*.json
# VITE_API_MODE=static
#
# Opcional: base para los JSON estaticos si se sirven de otro dominio/CDN
# (default: mismo origen, ej. /api)
# VITE_STATIC_BASE=https://mi-cdn.example
